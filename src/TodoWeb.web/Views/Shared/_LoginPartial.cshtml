@using Microsoft.AspNetCore.Identity
@using Todoweb.data.Entities
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<!--@if(SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
       <form asp-action="post">
              <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Logout">Logout</a>
       </form>  
    </li>
}
else
{
      <li class="nav-item">
            <form action="get">
                  <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Register">Register</a>
            </form>
      </li>
      <li class="nav-item">
            <form action="get">
                  <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Login">Login</a>
            </form>
      </li>
}-->

@if(SignInManager.IsSignedIn(User))
{
    <div class="button navbar-right">
        <form asp-action="post">
            <button class="navbar-btn nav-button wow bounceInRight login" asp-controller="Account" asp-action="Logout" data-wow-delay="0.4s">Logout</button>
        </form>
    </div>
}
else
{
    <div class="button navbar-right">
        <form action="get">
            <button class="navbar-btn nav-button wow bounceInRight login"  data-wow-delay="0.4s" asp-controller="Account" asp-action="Login">
                Login
            </button>
            <button class="navbar-btn nav-button wow fadeInRight" data-wow-delay="0.5s" asp-controller="Account" asp-action="Register">
                Register
            </button>
        </form>
        
    </div>
}